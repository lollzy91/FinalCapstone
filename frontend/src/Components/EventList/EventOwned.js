function EventOwned(props)
{
    return( 
        <div id = 'OwnedList'>
            <p>THIS IS THE OWNED LIST</p>
        </div>
    )
        //Body
        //LI of the events owned
        //Each one selected routes to specific ID of event
        //Changes to finale page after time
}


export default EventOwned