function EventInvited(props)
{
    return( 
        <div id = 'InvitedList'>
            <p>THIS IS THE INVITED LIST</p>
        </div>
    )
        //Main body
        //LI of the events owned
        //Each one selected routes to specific ID of event
        //Alerts if time is passed
}


export default EventInvited